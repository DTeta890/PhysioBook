# Next Task

Read `ROADMAP.md` and find the first task with status ⬜ whose dependencies are all ✅.

1. Announce which task you're picking up (ID + title)
2. Check if the task has a detailed file in `tasks/` — if yes, read it
3. Create a git branch: `feature/{TASK-ID}-{short-description}`
4. Determine which agent(s) are needed based on the task domain
5. Present a brief implementation plan (max 5 steps)
6. Ask for confirmation before starting
7. After completing, run verification checks from CLAUDE.md
8. Update the task status in `ROADMAP.md` from ⬜ to ✅
9. Commit with message: `feat: {task title} (TASK-ID)`
